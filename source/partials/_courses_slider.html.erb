<!-- slider block -->
      <div class="slider-wrapper theme-default">
        <div id="slider" class="nivoSlider">
          <!-- slider img place here -->
          <% data.index.slider.each do |f| %>
            <img class="img-responsive" src="<%= f.img %>" alt="" title="test">
          <% end %>
        </div>
      </div>
